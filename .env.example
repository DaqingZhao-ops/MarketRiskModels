# Optional URL of the separately hosted Python risk service.
# When omitted, the web interface uses its TypeScript continuity engine.
PYTHON_RISK_API_URL=http://localhost:8000
# Backup for the hosted Next.js market-data route. Keep the key in .env.local.
POLYGON_API_KEY=
